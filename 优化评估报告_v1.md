# Antigravity 汉化项目优化评估报告 v1

> 评估日期:2026-08-30
> 评估范围:`localization_engine.js`、`src/` 三个源文件、`scratch/` 质量工具、`dicts/` 结构、git 工作区状态
> 评估方式:全量通读核心代码 + 静态推演运行期路径 + 与现有测试覆盖对照

---

## 实施状态(2026-08-30 更新)

本报告 P0/P1/P2 各项已全部实施完毕,共 8 个提交,引擎版本 1.1.0 → 1.2.0,最终回归 **259 项测试 + 四道字典质量门禁全绿**:

| 项 | 状态 | 提交 |
| :--- | :--- | :--- |
| P0-3 快照回退点 | ✅ | `9a03265` |
| P0-1 hidden 品牌模式运行期失效 | ✅ 11 处空串吞噬点改为 null 哨兵,新增 7.5 节运行期回归 | `1654a32` |
| P0-2 README 失效引用 | ✅ 恢复 `维护工具.bat`(方案 A) | `bd9252f` |
| P2-1/3/4 工程化 | ✅ 字典单次加载;质量工具补退出码并入 npm test;ESLint 基线(0 error) | `044f852` |
| P1-1/2/3/4 微优化 | ✅ 中文短路、无属性跳过、norm 快速通道、查表前移 | `5a9d2a6` |
| P1-5 首词分派重构 | ✅ 先建 80 项 A/B 护栏,再重构,前后输出逐字符一致 | `04ef637` + `4601242` |

未实施(有意保留):P2-2 字典 asar 内嵌 3 份(评估结论:维持现状的已知取舍);P1-6 流式 closest 热点(按报告建议仅观察,量化有瓶颈再立项)。

---

## 一、总体结论

项目整体架构成熟度**高**,以下设计无需推翻重做,直接沿用:

- 单点全局拦截(asar 内仅挂 `antigravity_i18n_core.js` + main.js 一行 require)
- WeakMap 缓存防重复翻译/防内存泄漏
- 双级队列调度(微任务 + requestIdleCallback 分片)+ 队列溢出全量兜底
- 禁区选择器隔离(代码编辑区/终端/AI 正文零开口)
- asar 安全机制(头部校验、官方备份防污染、打包失败自动回滚)
- 168 项自动化回归测试 + 6 个字典质量工具
- 源文件与宿主拼装分离(`src/*.src.js` 可独立维护)

真正的优化空间集中在:**1 个运行期 Bug、1 处文档失效、若干边际收益的性能微优化、工程化闭环缺口、以及未提交的仓库状态风险**。逐项如下,按优先级分级。

| 编号 | 类别 | 问题 | 优先级 | 修复风险 | 收益 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| P0-1 | Bug | hidden 品牌模式运行期失效 | 高 | 低 | 功能正确性 |
| P0-2 | 文档 | README 引用已删除的 维护工具.bat | 高 | 无 | 用户可用性 |
| P0-3 | 流程 | 大量重构未提交,无回退点 | 高 | 无 | 安全网 |
| P1-1 | 性能 | 属性翻译自激重扫 + 中文短路缺失 | 中 | 低 | 高频交互路径 |
| P1-2 | 性能 | 无属性元素 7 次 getAttribute 空转 | 中 | 无 | 首屏扫描 |
| P1-3 | 性能 | lookup 重复 norm | 中 | 无 | 全路径微收益 |
| P1-4 | 性能 | 快捷键正则先于字典查表 | 中 | 低 | 全路径微收益 |
| P1-5 | 性能 | 动态句式 50+ 顺序正则 | 中 | 中 | 漏译多页面 |
| P2-1 | 工程 | 字典加载 2 次,警告打印两遍 | 低 | 无 | 输出整洁 |
| P2-2 | 工程 | 字典在 asar 嵌 3 份 | 低 | — | 建议不改,记录取舍 |
| P2-3 | 工程 | 质量工具未纳入 npm test 门禁 | 低 | 无 | 防回归 |
| P2-4 | 工程 | 无 ESLint 配置 | 低 | 无 | 可维护性 |
| P2-5 | 稳健 | asar 头长度上限 256MB 过宽 | 低 | 无 | 抗损坏输入 |

---

## 二、P0 级:应尽快处理

### P0-1 `--brand-title hidden` 模式运行期失效(疑似已发布 Bug)

**结论:用户选择"隐藏品牌名"后,界面上的 Antigravity 不会被隐藏,功能整体失效。**

证据链(空字符串译值在整条管线中被当作"未命中"):

1. 构建期写入空值:[localization_engine.js:94](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/localization_engine.js#L94)
   `totalMap[normalizeText('Antigravity')] = '';`
2. 运行期 `lookup` 用 `||` 链返回,`''` 为假值 → 返回 `null`:[translate_kernel.src.js:21](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/translate_kernel.src.js#L21)
   `return map.get(n) || lowerMap.get(n.toLowerCase()) || null;`
3. `translateString` 真值判断继续吞空串:[translate_kernel.src.js:546-547](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/translate_kernel.src.js#L546-L547)
   `if (exactTrans) return exactTrans;`
4. 渲染层文本回退原文:[renderer_engine.src.js:293](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/renderer_engine.src.js#L293)
   `const newVal = transRes || originalVal;`
5. 属性路径跳过写入:[renderer_engine.src.js:187](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/renderer_engine.src.js#L187)
   `if (trans && trans !== v)`
6. 主进程路径同样失效:[main_core.src.js:12-13](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/main_core.src.js#L12-L13)
   `if (kernelTrans) return kernelTrans;`

**附带影响**:该词未命中后进入漏译采集池([renderer_engine.src.js:309-312](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/renderer_engine.src.js#L309-L312)),污染 `dump_missing.js` 导出结果。

**测试盲区**:[engine_fix_test.js:579](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/scratch/engine_fix_test.js#L579) 只断言了构建期字典里存了 `''`,从未走运行期 `lookup → translateString → DOM 回填` 路径,所以 168 项测试全部绿灯但功能是坏的。

**修复方案**(多点小改,注意 `''` 与 `null` 必须在语义上区分开):

```js
// 1. translate_kernel.src.js — lookup 区分"缺失"与"空值"
function lookup(s) {
    if (!s) return null;
    const n = norm(s);
    if (!n) return null;
    if (map.has(n)) return map.get(n);
    const l = n.toLowerCase();
    if (lowerMap.has(l)) return lowerMap.get(l);
    return null;
}
// 2. translateString — 精确命中改为显式 null 判断
if (exactTrans !== null) return exactTrans;
// 3. renderer translateTextNode —
const newVal = (transRes === null ? originalVal : transRes);
// 4. renderer translateElementAttrs —
if (trans !== null && trans !== v) node.setAttribute(attr, trans);
// 5. main_core translateText —
if (kernelTrans !== null) return kernelTrans;
```

**边缘情况需在实现时决策**:

- 复合标题 `"New chat — Antigravity"` 在 hidden 模式下后段译为 `''`,`translateCompoundTitle` 会产出悬挂分隔符(如 `"新聊天 — "`),需对空段做合并/裁剪。
- `translateWithShortcut`、`translateTaskTarget` 等内部调用点的 `if (trans)` 真值判断同样会受空值影响,实现时应逐一过一遍(现实中这些路径的键值不会是 `''`,风险低,但值得确认)。
- 空值修复后,`translateElementAttrs` 的属性值翻译结果为 `''` 时 DOM 会写入空属性值,符合"隐藏"预期,但 `title=""` 在部分浏览器会移除 tooltip,行为符合预期。

**回归测试建议**(补进 engine_fix_test):

- 用 `buildMod(…, 'hidden')` 构建内核源码,vm/jsdom 中执行,断言 `translateString('Antigravity') === ''`;
- 断言 hidden 模式下含 `Antigravity` 文本节点翻译后 `nodeValue === ''`;
- 断言 hidden 模式下 `missedTexts` 不含 `Antigravity`。

---

### P0-2 README 引用已删除的 `维护工具.bat`

git 工作区状态显示以下文件已删除(未提交):`维护工具.bat`、`修复验证报告_v3.md`、`排查报告_翻译覆盖与安全区隔离_v2.md`。

但 [README.md:13](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/README.md#L13) 快速使用表格第三行,以及第 241 行起"维护与质量保证工具"整节,均引导用户运行 `维护工具.bat`(交互菜单运行 6 个诊断脚本)。**用户照文档操作会直接踩空。**

处置二选一:

- **方案 A(推荐)**:`git checkout -- 维护工具.bat` 恢复脚本。对双击 bat 的目标用户(非命令行人群)有实际价值。
- **方案 B**:确认弃用后,把 README 该节改为 `node scratch/dict_check.js` 等直跑说明,并提交删除。

不能维持现状(文档指向不存在的入口)。

---

### P0-3 仓库状态风险:重构未提交

`git status` 显示:

- `src/` 整个目录**未跟踪**(源码拆分重构未入库)
- `scratch/apply_missing.js` 未跟踪
- `README.md`、`localization_engine.js`、`package.json`、`scratch/engine_fix_test.js` 已修改未提交
- 3 个文件删除未提交

当前**没有可回退的快照**。任何后续优化动手前,应先提交现状(一次快照提交,再按项分批提交),否则出问题无法定位与回滚。

---

## 三、P1 级:性能微优化(如实评估:引擎本身已经很快,以下均为边际收益)

README 性能章节的宣称(TreeWalker 熔断、微任务即时、分片调度)与代码实现相符。以下按收益/风险排序。

### P1-1 属性翻译自激重扫 + 中文短路缺失(收益最实在的一项)

**现状链路**:每次属性翻译 `node.setAttribute(attr, trans)` 会:

1. 进入被 patch 的 `setAttribute` 拦截器 → 再跑一次 `closest(ALL_BLOCKED_SELECTOR)`(100+ 选择器的大串,见 [renderer_engine.src.js:126](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/renderer_engine.src.js#L126));
2. 异步触发 MutationObserver `attributes` 回调 → 对同元素 **7 个可翻译属性全部重跑翻译管线**(每个已含中文的值都要走 norm + 4 个快捷键正则 + 字典查表 + 50+ 个动态正则,全失败后返回)。

**注意**:`isMutating` 标志**无法**阻断这条自激链——MutationObserver 回调在当前同步代码结束后的微任务检查点派发,彼时标志已复位。引擎防死循环实际靠的是"中文值翻译必失败 → 不再写入"这一有界性,链路安全但浪费。

**修复**(小改动,双收益):

- 在 `translateAttrValue`(或 `translateString` 顶部)加与 `setAttribute` 拦截器一致的中文短路:

```js
function translateAttrValue(v) {
    if (!v || typeof v !== 'string') return null;
    if (/[\u4e00-\u9fa5]/.test(v)) return null;  // 已含中文,直接短路
    return translateString(v, null);
}
```

已译属性的重扫代价从 ~60 次正则降为 1 次,同时消除了拦截器路径(有中文检查)与观察器路径(没有)的**行为不一致**。

- **实现时需确认**:动态句式里存在 `(.+?): context deadline exceeded` 这类允许前缀含中文的混合文本模式。若把短路加在 `translateString` 全局,这类"中文前缀 + 英文尾部错误码"的节点将不再翻译尾部;若现实中该形态只出现在错误详情(通常在禁区内),加在全局也安全。稳妥起见可先只加在 `translateAttrValue`(属性路径)。

### P1-2 无属性元素快速跳过

[translateElementAttrs](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/renderer_engine.src.js#L181) 对每个元素节点固定执行 7 次 `getAttribute`。首屏全量扫描中大量节点是无属性的 `div`/`span`。顶部加一行:

```js
if (!node.attributes || node.attributes.length === 0) return;
```

零行为变化,纯省空转。

### P1-3 `lookup` 重复归一化

`translateString` 已 `norm(text)` 得到 `valNorm`,`lookup` 内部又对同一字符串再跑一遍 `norm`(1 个空白折叠 + 4 个引号替换 + trim,共 6 次正则过程)。每个文本节点白白多跑 6 次。方案:

- `lookup` 拆出 `lookupNorm(n)`(跳过内部 norm)供已归一化调用点使用;或
- `norm` 顶部加快速通过判断(无连续空白、无弯引号、无首尾空白时直接返回原串)。

绝对耗时很小(每节点微秒级),但改动同样小,顺手可做。

### P1-4 快捷键正则先于字典查表

[translate_string 顺序](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/translate_kernel.src.js#L544-L547):每个字符串先跑 `translateWithShortcut` 的 4 个正则,然后才查 O(1) 的 Map。把 `lookup` 提前可让绝大多数字典命中串跳过 4 个正则。

**前置校验**:需先用 dict_check 确认字典中不存在"完整括号键"(如 `"Save (Ctrl+S)"` 整键)且其译值与快捷键重组结果不一致的情况——若有,顺序调换会改变这些键的译文来源。无冲突即可安全调换。

### P1-5 `translateDynamicText` 50+ 顺序正则 → 首词分派

[translateDynamicText](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/src/translate_kernel.src.js#L166-L537) 是一个 370 行、约 50 个 `if (/regex/.test(valNorm))` 顺序块。每个**未命中字典**的字符串要把 50 个正则全跑完才放弃。

可改为按首词(第一个空格前的 token)建立 `Map<word, pattern[]>` 分派表,常规串只测试 1~3 个模式。收益集中在漏译较多的页面(每个未知串从 ~50 次正则降到常数次)。

**风险与成本**:改动面大,但 168 项回归测试覆盖了动态句式分支,可护航。属于"值得做但应单独一个批次 + 全量回归"的项。

### P1-6 流式输出场景的 `closest` 热点(仅观察,暂不建议做)

AI 流式输出期间,`characterData`/`childList` 突变每次都要对目标跑一次 `closest(ALL_BLOCKED_SELECTOR)`(命中 `[data-is-streaming]` 等禁区后跳过)。高频突变时这是重复的大选择器匹配。

理论上可用 WeakSet 记忆已判定为禁区的元素跳过重复 `closest`,但**元素重挂载/移动后缓存会过期 → 漏译风险**,与"彻底杜绝漏译"的项目原则冲突。建议先实测量化(流式期间 Performance 面板观察 observer 回调占比),确有瓶颈再设计带失效机制的方案。

---

## 四、P2 级:构建与工程化

### P2-1 字典重复加载 2 次,冲突警告打印两遍

[install20](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/localization_engine.js#L503) 步骤 4 `generateI18nCoreJs()` 内部 `loadDictionary()` 一次;步骤 6 [generateJs()](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/localization_engine.js#L529) 未传参又加载一次。效果:~165KB JSON 读盘解析 ×2,且每个跨文件键冲突警告在安装日志里出现两遍,容易让用户误以为冲突更严重。

修法:`install20` 顶部 `const dict = loadDictionary();` 后贯穿传参给 `generateI18nCoreJs(dict)` 与 `generateJs(dict)`。

### P2-2 字典 JSON 在 asar 内嵌 3 份(评估结论:不改)

三份位置:main_core 内核、main_core 内嵌的 `RENDERER_CODE_PLACEHOLDER` 渲染串、preload.js。冗余约 2 × 165KB ≈ 330KB,相对数百 MB 的 app.asar 可忽略。

且 preload 运行于 contextIsolation 沙箱,无 `fs` 可用,字典只能内嵌;外置需引入新的注入通道,复杂度不划算。**建议记录为已知取舍,维持现状。**

### P2-3 质量工具未纳入 `npm test` 门禁

现状:`npm test` 只跑 `engine_fix_test.js`。6 个字典工具游离在门禁之外,字典回归只能靠手动运行 维护工具.bat(而该 bat 目前还处于删除状态,见 P0-2)。

核实结果:

- `dict_check.js` **有** `process.exit(1)` 退出码,可直接串联;
- `dict_quality_check.js` 只打印**不设退出码**,直接并入会产生假绿;
- `mainproc_keys_check.js`、`enum_group_check.js` 需同样确认。

建议:先给缺退出码的脚本补 `process.exit(bad ? 1 : 0)`,然后:

```json
"test": "node scratch/engine_fix_test.js && node scratch/dict_check.js && node scratch/dict_quality_check.js && node scratch/mainproc_keys_check.js && node scratch/enum_group_check.js"
```

### P2-4 无 ESLint 配置

README 宣称 `src/` 源文件"可 lint、可测试、可调试",但仓库无 eslint 配置、无 lint script。补最小配置(`env: browser + node`、`es2022`、少量规则)加 `"lint": "eslint src localization_engine.js"` 即可兑现承诺,防止低级语法/风格回归。

### P2-5 asar 头长度上限过宽

[readAsarHeaderSize](file:///c:/Users/geniu/Desktop/Desktop/antigravity_cn_test/localization_engine.js#L230) 允许 headerSize 最大 256MB,`isHanhuaAsar` 会按此 `Buffer.alloc(8 + headerSize)`。遇到构造的损坏包,极端情况单次分配 256MB。真实 asar 头仅几 MB,上限收紧到 16MB 足够,更抗恶意/损坏输入。

---

## 五、建议实施顺序

1. **先提交当前快照**(P0-3):一次快照提交收录 src/ 拆分重构与全部改动,建立回退点。
2. **P0-1**:修 hidden 品牌模式(5 处小改 + 1 处复合标题边缘处理),同批补运行期回归用例。
3. **P0-2**:恢复 `维护工具.bat` 或改写 README,二选一。
4. **P2-1 + P2-3 + P2-4**:三个低风险工程项合成一批,半小时量级的改动,形成质量门禁闭环。
5. **P1-1 + P1-2 + P1-3**:渲染层三个小改微优化,一批做完跑全量回归。
6. **P1-4**:前置跑 dict_check 确认无括号整键冲突后调换查表顺序。
7. **P1-5**:首词分派重构,独立批次 + 全量回归,最后做。
8. **P1-6**:仅观察,量化有瓶颈再立项。

## 六、附录:验证命令

```bash
# 全量回归
npm test

# 字典结构检查(有退出码)
node scratch/dict_check.js

# 修复 hidden 后的针对性验证(建议补入测试)
#   buildMod(…, 'hidden') → vm 执行内核 → translateString('Antigravity') === ''
#   jsdom:文本节点 "Antigravity" 翻译后 nodeValue === ''

# 当前仓库状态
git status --short
```
