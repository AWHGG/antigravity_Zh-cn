# 修复验证报告（v3 · 全量回归 + 对抗性边界复核）

- **对象**：`localization_engine.js` 修复后的工作区版本（相对 HEAD +154/-28 行）、`scratch/engine_fix_test.js`（+76/-11 行）、README、dicts/common.json
- **验证方法**：
  1. `git diff` 逐项比对 v2 报告条目与修复改动；
  2. 官方回归套件 `npm test`：**116 通过 / 0 失败**；
  3. 独立对抗性边界验证 19 项（套件未覆盖的裸类型、未加门禁句式、属性路径组合、溢出、shadow 禁区），**10 通过 / 9 失败**；
  4. 环境相关结论基于官方包实证（`contextIsolation: true`）逻辑推断，jsdom 同世界测试无法覆盖。
- **日期**：2026-08-29

---

## 一、总览：v2 报告条目 × 修复状态

| 条目 | 修复状态 | 证据 |
|---|---|---|
| C1 队列溢出静默丢弃 | ✅ **已修复** | else 分支即时兜底（`engine:1036-1047`）；套件 2.5 改 250 节点通过；对抗性验证 250 节点全译 |
| C2 删除确认默认名词 + `?` 吞名 | 🟡 **部分修复** | 默认名词/冠词已修；**裸类型 `?` 吞名残留**（实测 4 例失败，见下） |
| C3 placeholder 属性链失效 | ✅ **已修复** | TreeWalker 对 BLOCKED_TAGS 先翻属性再 REJECT + observer attributes 分支对 input 豁免 + 补丁禁区检查。**修复路径全部 world 无关**，不再依赖 setAttribute 补丁 |
| C4 属性覆盖集合三处不同步 | ✅ **已修复** | `TRANSLATABLE_ATTRS` 统一（扫描/拦截/observer 三处）；实测静态 `data-tooltip` 初始扫描即翻译 |
| C5 document.title 无覆盖 | 🟡 **部分修复** | **初始标题** ✅（实测 "Settings - Antigravity" → "设置 - Antigravity"）；**动态标题在真实环境仍不生效**（见环境残留） |
| C6 标点变体漏译 + 半翻译输出 | 🟡 **部分修复** | catch-all 加长句门禁（7 个句式）；**门禁不完整**（实测 5 类未加门禁句式仍被改写）；标点变体（`(1,024)` 等）未动 |
| C7 主进程盲区 | ✅ **已修复** | `checkboxLabel`/`nameFieldLabel`/`Menu.prototype.popup`；套件 3 区 PASS |
| C8 主/渲染动态规则双实现 | ❌ **未修复** | 主进程 `translateText` 仍仅 4 条规则（P2） |
| C9 单空格保真 | ❌ **未修复** | （P2） |
| C10 框架拆分节点上限 | ❌ **未修复** | 设计上限，建议保留（P2） |
| C11 URL 白名单子串匹配 | ✅ **已修复** | 改为 `new URL().hostname` 精确判断 |
| S1 AI 正文隔离 + 长句门禁 | 🟡 **部分修复** | 选择器扩展 ✅（`[class*="markdown"]` 等 + `[data-message-author="assistant"]` + 流式裸属性）；**第二道防线（长句门禁）只覆盖 7 个句式**；新增过挡风险 N1/N2 |
| S2 用户内容未整体隔离 | ❌ **未修复** | 用户消息容器仍未整树入禁区（P1 残留） |
| S3 属性补丁无禁区检查 | ✅ **已修复** | `setAttribute`/`title` 补丁均加 `closest(ALL_BLOCKED_SELECTOR)`；实测禁区内 setAttribute/`el.title` 赋值均不翻译，禁区内 input placeholder 豁免翻译 |
| S4 整树过挡（tool-call-result 等） | ❌ **未修复** | （P2） |
| S5 子串选择器过杀 | ❌ **未修复** | 需 dump 实证后收敛（P2） |
| S6 shadow 单层/枚举穿透 | ✅ **意向变更** | 深层常规宿主 shadow 改为递归翻译（测试 2.4 同步更新），monaco/xterm 宿主不穿透（实测 ✅）；残留：MO 不跨 shadow 边界，shadow 内动态更新仍不重译（P2） |
| S7 isMutating 失效 | ❌ **未修复** | 注释与实现不符（P2） |
| S8 物理特征小项 + README 22/23 | 🟡 **部分** | README 已改 23 个 ✅；hex 误杀/扩展名缺项未动（P2） |

**统计：✅ 7 项 / 🟡 5 项 / ❌ 8 项（其中 S2 为 P1 残留，其余多为 P2）**

---

## 二、对抗性边界验证：残留问题（实测证据）

### R1【P0 残留】C2 删除确认「裸类型 + 结尾问号」仍错译 — 套件 2.8 恰好未覆盖

正则 `(.+?)\??$` 未改，类型组匹配后剩余仅 `?` 时，`?` 被吞进名称捕获：

| 输入 | 实测输出（仍错） |
|---|---|
| Are you sure you want to delete this project? | 您确定要删除此项目 **?** 吗？ |
| Are you sure you want to delete the project? | 您确定要删除该项目 **?** 吗？ |
| Are you sure you want to delete project? | 您确定要删除项目 **?** 吗？ |

（"…delete this workspace?" 因字典有精确键而译对；套件 2.8 的 5 个用例全部带真实名称，故全绿。）
**修复**：捕获后 `name.replace(/\?+$/, '')` 清洗 + 空名称走"您确定要删除此{类型}吗？"句式；补上述 3 例回归用例。

### R2【P1 残留】S1/C6 长句门禁不完整 — 5 类宽泛句式实测仍改写长句

`isLongProse` 只挂在 Ran/Checked/Checking/Killed/Killing/Run/Updated 7 个句式上，以下宽泛句式未加门禁，长英文段落仍被改写为混串：

| 实测输入（长句） | 实测输出（被改写） |
|---|---|
| Searched the workspace for all references to the deprecated API… | 已搜索 the workspace for all references… |
| A comprehensive analysis of the request across all files finished | A comprehensive analysis… 已完成 |
| Checked task 42: verify all tests pass before merging the feature branch. | 已检查任务 42: verify all tests… |
| Status: all systems operational across the entire deployment fleet | 状态：all systems operational… |
| Learn more about whether the agent should run tools in your project workspace | 了解更多关于 whether the agent should run tools… |

说明：真实环境中这些句子若已落入 AI 禁区选择器则不会到达句式层，此残留属于**第二道防线不完整**。`finished`/`Searched`/任务动词/`Status:`/`Learn more about` 均应复用 `isLongProse` 门禁（或把门禁上移到 `translateDynamicText` 入口统一短路）。

---

## 三、新引入的问题（本次修复带来的副作用）

| 编号 | 级别 | 问题 | 说明 |
|---|---|---|---|
| N1 | P1（需实证） | `[class*="markdown"]`、`[class*="rich-text"]`、`[data-message-author="assistant"]` 整树过挡 | 设置页"Markdown 预览"类选项、富文本 UI、assistant 消息内的操作按钮（复制/重新生成等）若落在这些容器内将永久英文——覆盖损失。同时它顺带修复了 artifact markdown 正文的欠隔离，属双刃，**需用 dump_missing 的 blocked 清单实证**哪个方向的损失更大 |
| N2 | P1（需实证） | `[data-is-streaming]`/`[data-streaming]`/`[data-is-generating]` 由 `.prose` 后代改为裸属性 | 若这些属性挂在高位祖先（如整个聊天视图根），流式期间输入框、模型选择器、发送按钮等 UI 全部保持英文。需实证属性实际挂载层级 |
| N3 | P2（性能） | 队列溢出 else 分支同步逐节点 `translateSubtree` | 数千节点批量（历史会话恢复）时会在单个微任务内同步扫描全部溢出节点，**绕过原分片设计产生长任务**。建议改为：溢出时对批次公共祖先做一次性兜底扫描（或限制单次兜底扫描次数） |
| N4 | P2 | TreeWalker 对 BLOCKED_TAGS 先翻属性 → 禁区内 INPUT/TEXTAREA 的属性（含 Monaco 隐藏 textarea 的 aria-label）不再受禁区限制 | 实测无实际影响（字典无匹配长 aria-label），但属隔离口径有意放宽，需知悉 |
| N5 | P2 | attributeFilter 扩展 7 属性 → data-* 变更触发 observer 属性分支频率上升 | 性能微升，可接受 |
| N6 | P2 | document.title 初始翻译与客户端 `page-title-updated` 25 字符截断流程并发 | 实测无冲突（截断在翻译后），低风险 |

---

## 四、环境相关残留（jsdom 覆盖不到的推断项）

官方包实证 `contextIsolation: true`（主窗口与 loadingOverlay）成立的前提下：

1. **动态 document.title 仍不生效（C5 残留）**：`Document.prototype.title` 补丁装在 isolated world，主 world 框架 `document.title = "New chat — Antigravity"` 走主 world 原型不会被拦截；且 observer 挂在 `body`，`<head>` 的 `<title>` 文本变更不被观察；主进程 `win.setTitle`（官方 `utils.js` 的 `page-title-updated` 截断处理器会调）也没有被 core hook。→ **初始标题会翻，一旦页面路由改变标题立即回到英文**。套件 2.10 通过仅代表同世界（jsdom）行为。
   - 修复方向：a) observer 目标改为 `document.documentElement`（覆盖 head 变更）；b) 主进程 core 增加 `BrowserWindow.prototype.setTitle` 包装 / `win.on('page-title-updated')` 翻译；二选一或都做。
2. **setAttribute/title 补丁在真实环境为"部分静默失效"**：主世界框架调用不触发，但同款属性已由 observer/扫描路径覆盖（C3 修复后不依赖补丁）——补丁退化为冗余加速路径，无害。
3. 上述两点无法在本机 jsdom 验证，建议以「客户端实际运行 + CDP 观察 title 变化」方式确认。

---

## 五、结论与下一轮建议

**已全量修复的**：C1、C3、C4、C7、C11、S3、S6（设计变更）、README/字典同步。116 项套件全绿，属性路径组合与 shadow 禁区一致性经独立验证通过。

**未全量修复的**（按优先级）：

- **P0**：R1 —— C2 裸类型 `?` 吞名（一行清洗 + 3 例回归）；
- **P1**：R2 —— 长句门禁上移到 `translateDynamicText` 入口统一短路（覆盖 `Searched`/`finished`/任务动词/`Status:`/`Learn more about`）；S2 —— 用户消息容器整体入禁区；C5 动态标题（observer 移 `documentElement` 或主进程 hook `setTitle`）；N1/N2 过挡实证（跑 `dump_missing` 看 blocked 清单）；
- **P2**：N3 队列兜底性能（公共祖先一次扫描）；C8 主进程动态规则复用；其余按 v2 报告记录。

---

# 六、v4 修复记录（2026-08-29 第二轮修复）

按 v3 报告 P0/P1 清单落地修复，**回归套件 134/134 通过**，**独立对抗性终验 29/29 通过**（含 3 轮队列溢出确定性复测）。

## 本轮改动（`localization_engine.js` 8 处 + 测试扩充）

| # | 对应条目 | 改动 |
|---|---|---|
| 1 | R1（C2 残留） | 删除确认句式：名称捕获后 `replace(/\?+$/, '')` 清洗；空名称走"您确定要删除此{类型}吗？"句式。套件 2.8 补 4 例裸类型回归（`this project?`/`the project?`/`project?`/`this workspace?`） |
| 2 | R2（长句门禁） | `isLongProse` 取消标点/换行要求（>6 空格即视为长句）；门禁扩展到 `Searched`/`(.+?) finished`/任务动词/`Status:`/`Learn more about` 5 类句式；`Searched` 同时接入 `translateCountList`（"Searched 3 files" → "已搜索 3 个文件"）。短句行为不受影响（实测 `Status: Fired` 等仍翻译） |
| 3 | N3（溢出兜底） | 溢出兜底从"逐节点同步扫描"改为**单次全树重扫**；调度优先级 `requestIdleCallback`（真实 Chromium）→ `queueMicrotask`（确定性执行）→ `setTimeout 0`。**修复过程中发现并规避了一个真实陷阱**：jsdom 环境下溢出重扫的 `setTimeout(16)` 定时器与分片链并发时会被吞（连续 3 轮确定性复现漏译 50 节点），微任务路径彻底消除该时序竞争 |
| 4 | C5（动态标题） | observer/首扫根节点改为 `documentElement`（覆盖 `<head>`）；`translateTextNode` 新增 TITLE 分支——`<title>` 文本直改（主 world 绕过 setter 的场景）经观察器走 `translateDocumentTitle` 分段翻译；标题节点不进 missedTexts（品牌词噪声）；主进程 core 新增 `BrowserWindow.prototype.setTitle` hook + `translateText` 复合标题分段（`New chat — Antigravity` → `新会话 — Antigravity`） |
| 5 | S2（用户内容） | 禁区新增 `[data-turn-role="user"]`/`[data-message-author="user"]`/`[data-role="user"]` 整树隔离；`[data-testid*="conversation-item"]` 由 truncate 后代提升为整行隔离 |
| 6 | C9（空格保真） | 首尾空白改为完整空白段保真（`/^\s+/`、`/\s+$/`），pre-line/pre-wrap 缩进不塌缩 |
| 7 | S8（扩展名） | 物理特征扩展名清单补 `.txt/.log/.env` |
| 8 | 测试 | 2.10 补观察器路径用例（`<title>` 文本直改）；新增 2.11 用户内容隔离、2.12 空格保真；core 断言补 `setTitle`/`compoundMatch` |

## 终验矩阵（29 项，全部通过）

- R1 裸类型删除确认 5 例；R2 长句门禁 5 类 + 短句不误伤 4 例；
- 队列溢出 250 节点 × 3 轮（确定性）；`<title>` 观察器路径；空格保真；用户内容 3 种标记 + 常规 UI 不受影响；
- 主进程 core：setTitle 复合标题 + popup + checkboxLabel；禁区属性路径（区内 title 不翻 / input placeholder 豁免翻译）。

## 仍留待的事项（非本轮范围）

- **N1/N2（P1，实证型）**：新增 `[class*="markdown"]`/`[data-message-author="assistant"]`/裸 `[data-is-streaming]` 选择器的过挡面，需在客户端运行后以 `dump_missing` 的 blocked 清单实证（本轮无法在无客户端环境下确认）；
- **C8（P2）**：主/渲染动态句式双实现未合并；**S4/S5（P2）**：tool-call-result 整树挡与子串选择器过杀未收敛；
- **C6 尾部**：`Updated 3 files` 类短串的计数段未接入（半翻译残留，低优先）。

---

# 七、复盘：对"入口一刀切短路/主世界 100% 拦截/整树隔离自相矛盾"三点的实证裁决（2026-08-29）

对 Gemini 反驳的逐条实测复核结论。证据脚本已清理，判定基于生成代码偏移量实测与官方套件行为：

## 1. isLongProse 入口短路 —— ✅ 认可（针对 v3 报告建议措辞），但最终实现与对方主张一致

- **实测（同一判定逻辑）**：三个 Antigravity 原生系统长模板若按"入口一刀切短路"执行会被全部误杀：
  - `This will permanently delete 5 active conversations within it.`（62 字符 / 8 空格）→ 会被杀；
  - `You have used some of your weekly limit, it will fully refresh in 2 hours 30 mins.`（82 / 16）→ 会被杀；
  - `The command exited with code 0. Output: ...`（94 / 14）→ 会被杀。
- **实测（当前实现）**：三条模板全部正常翻译（"此操作将永久删除其中的 5 个活跃会话。"等）——v4 落地采用的是**逐句式门禁**（仅挂载于 Ran/Searched/Checked/Killing/Run/Updated/任务动词等 10 个宽泛动词句式），与对方主张的"只能精准施加在开放式动词句式上"完全一致。
- **裁决**：v3 报告第 103 行"上移到 translateDynamicText 入口统一短路"是坏建议，**认**；最终代码未采纳该建议，无此缺陷。

## 2. document.title 主世界拦截 —— ❌ 不认可"100% 实时拦截"

- 对方正确的一半：`webContents.executeJavaScript` 默认运行于主 world。
- 被忽略的变量：**互斥锁**。生成代码实测偏移——`data-ag-i18n-active` 互斥检查位于字符偏移 **337**，`setAttribute` 补丁在 **147269**、`document.title` 补丁在 **149448**、observer 在 **158806**。互斥 return 远先于任何补丁安装。
- 现实链：preload（isolated world，官方包实测 `contextIsolation: true`）先执行并打标 → executeJavaScript（主 world）后执行，在补丁安装之前 return——**主 world 的 Document.prototype/Element.prototype 从未被补丁**，框架 `document.title =` 走原生原型，拦截不到。执行环境（主 world）为真 ≠ 引擎曾运行到补丁安装处。
- 对方承认且 v4 已修的点（observer 未监听 `<head>`）正是观察器路径修复（documentElement + TITLE 分支），该路径 world 无关，是更稳的覆盖方式。
- 条件性说明：若按 C3 建议③把互斥锁改为"主 world 仍安装补丁"，对方的"主 world 拦截"推断才会成立——这是未来改法下的分歧，不是当前实现的事实。

## 3. S2 与 N1/N2 的关系 —— ⚠️ 部分认可：粒度缺口真实，"自相矛盾"不成立，其解法会引入新问题

- "自相矛盾"不成立：S2 针对**用户消息**（`[data-turn-role="user"]`/`[data-message-author="user"]`——用户数据绝不可翻，内容污染优先级高于覆盖损失）；N1/N2 针对 **assistant/markdown/streaming 容器**（AI 内容 vs UI chrome 的边界问题，可实证后收敛）。两个对象、两种风险性质；N1/N2 是评审对开发者新增选择器的风险标注，与"整树隔离"推荐是同一权衡的两面，而非矛盾指令。
- 认可的部分：**"整树熔断 vs 容器内嵌 UI 控件翻译"是机制级粒度缺口**（当前引擎只有"全挡/全放"），值得作为架构方向记录。
- 不认可其解法："对禁区内 button/[role=button] 细粒度放行"会把 **AI 输出内容里内嵌的按钮（AI 生成的 HTML/预览中的"内容"按钮）** 一并放行翻译，S1 风险回潮。正确判别维度是"是否应用 UI chrome"（data-testid/稳定类名/tool-call 结构内），需要结构级白名单机制，复杂度远高于标签放行。

## 共识项（双方一致）
R1（删除确认 `?` 吞名）、测试套件对抗性用例缺失、`<title>` 观察器路径改进方向。

---

# 八、v5 运行时实证记录（2026-08-29 真实客户端 CDP 验证）

实证环境：本机 Antigravity 客户端（汉化版已安装）以 `--remote-debugging-port=9333` 启动，CDP 连接 `dump_missing.js` 与自研探针扫描。

## 架构事实（静态解包确认）

- **Antigravity 为"瘦壳"架构**：`app.asar` 仅含主进程（dist 25 文件）与 node_modules，**渲染层不在 asar 内**；主窗口加载 `https://127.0.0.1:<动态端口>/`，UI 由 `bin/language_server.exe`（146MB Go 二进制）伺服；
- 渲染层代码在二进制的可读字符串区**不可见**（类名零命中）——**静态查验渲染层类名不可行，CDP 运行时扫描是唯一实证途径**；
- 包内三个 asar 的真相：`app.asar`（汉化版）与 `app.asar.bak2`（汉化版备份）同尺寸 21.9MB；`app.asar.bak`（4.5MB）为干净原版。

## 实证结论（逐条）

| 项 | 结论 |
|---|---|
| **N1/N2 过挡风险** | **证伪（当前样本）**：会话列表 + 项目视图 + 设置面板 74 个按钮全量汉化（通用/应用/外观…），`[class*="markdown"]`/`[data-message-author]`/`.prose`/`code`/`pre` 等引擎选择器**零命中**，blocked 疑似 UI 文案 0 条。AI 流式正文视图未实证（需真实对话消耗用户资源，留待用户日常使用观察） |
| **S2 用户内容隔离** | **实锤失效后修复**：真实会话标题结构为 `SPAN.truncate ← DIV[data-testid=conversation-row-sidebar]`，引擎的 `a[href*="/c/"]` 与 `[data-testid*="conversation-item"]` **全部零命中**（测试 id 为 `conversation-row-sidebar`/`conversation-list-sidebar`）。修复：新增 `[data-testid*="conversation-row-sidebar"] [class*="truncate"]` 与 `[data-testid*="conversation-list-sidebar"] [class*="truncate"]`（只挡标题，行内时间"45分钟前"等元数据仍翻译）。**活体注入验证**：10 个会话标题全部保持英文原样且退出漏译池 ✓ |
| **identity 键池污染** | **实锤后修复**：`Antigravity`（english 品牌模式删键）与 `Gemini 3.7 Flash`（identity 键）因"翻译后同值"反复进入漏译池（dump 误报"有键未命中"）。修复：① english 模式改为 identity 键（值=原文）；② 漏译池收集条件增加 `!/[\u4e00-\u9fa5]/`（滤除已译但含 ASCII 词的文本）。**活体验证**：漏译池 80→19 条，`Antigravity` 与 `Gemini 3.7 Flash` 均出池 ✓ |
| **时间戳单节点形态** | **补强**：客户端字典以分片键覆盖（"minutes ago"→"分钟前"），拆分渲染时正常；但单节点 `3 hours ago` 无句式。新增完整句式 `^(\d+)\s+(seconds?|minutes?|hours?|days?|weeks?|months?|years?)\s+ago$` ✓ |
| **工具缺陷** | `verify_fix_live.js` 清理时遗漏 `data-ag-i18n-active` DOM 互斥锁 → 新引擎注入会直接早退（等于没注入）。已修复并注释说明 |

## 验证后状态

- 官方套件 **139/139** 通过（新增 2.13 真实结构会话标题隔离、2.14 identity 不进池；品牌断言同步更新）；
- 活体注入终验：会话标题全隔离、identity 出池、漏译池降噪至 19 条残余（多为缺键的真实英文与设置面板内项目名，属合理采集）；
- 说明：活体注入仅影响当前运行会话；重启客户端后引擎回到安装包版本。**要让安装的客户端带上本轮全部修复（含 v4/v5），需重跑 `双击安装中文汉化.bat` 或 `node localization_engine.js` 重新注入**。

## 遗留（实证后收敛决定）

- N1/N2 的 AI 流式视图实证：留待日常使用时观察（发送真实对话）；
- 设置面板内项目列表项（"antigravity_cn_test" 残留于池）不在 conversation-sidebar 容器内，字典无键时不翻译，可接受。

*（v5 全部临时探针脚本与解包目录已清理；客户端保持运行未改动，可自行关闭。）*

*（本节为复盘结论追加，不修改 v3/v4 历史记录；全部证据已实测，临时脚本已清理。）*