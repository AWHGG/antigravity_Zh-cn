# Antigravity 汉化引擎排查报告（v2 · 合并复核版）

- **评审对象**：`localization_engine.js`（1888 行，渲染层引擎 + 主进程 core + 安装器）、`dicts/` 字典体系、`scratch/` 维护工具、README 声明
- **评审标准**：仅以 ① 翻译覆盖面 ② 安全区隔离覆盖面与精确度 两项为准
- **复核方法**：源码通读 → grep 全文交叉验证 → Node/PowerShell 正则实测 → 解包本机官方 `app.asar.bak` 实证运行环境 → 对照 `engine_fix_test.js` 已锁定的设计边界
- **日期**：2026-08-29
- **版本说明**：本报告合并两轮独立评审结论，全部条目经代码复核或实测验证；GLM 评审中 2 项增量发现（C1、C2）已验证采纳，其 contextIsolation 断言经官方包实测修正（见 C3）。

---

## 总体结论

架构主体健全：TreeWalker 硬熔断、双级调度、WeakMap 缓存、主进程单点 Hook、asar 备份/回滚分层均无结构性问题；字典 2,566 条经归一化后全部唯一、零跨文件冲突、零 lowerMap 冲突（25 条纯英文 identity 键为刻意设计）。README 文档口径存在 2 处与实现不符（队列兜底、BLOCKED_TAGS 数量）。

但按两项标准衡量，存在 **4 项 P0（含 1 项已由官方包实证的确定性缺陷）、5 项 P1、10 项 P2**，详见下文。**最关键的环境事实：官方主窗口 `contextIsolation: true`（已解包官方包验证），这会令"属性翻译链"整条失效（C3），是本次排查最重要的升级结论。**

---

# 一、翻译覆盖面（会漏译/错译的地方）

## C1【P0】增量队列 200 上限溢出后静默丢弃，README 声称的兜底不存在 —✅ 代码复核确认

- **位置**：`localization_engine.js:946`、`:954`；README「内存占用分布」表
- **问题**：两处入队判断 `if (pendingQueue.length < 200)` **均无 else 分支**。grep 全文确认引擎内不存在任何溢出补偿扫描。README 声称"超出部分丢弃多余引用并通过子树全量遍历兜底"，该兜底在注入代码中**不存在**。
- **影响**：历史会话恢复、设置页首开、长列表虚拟化等单批 >200 节点的渲染，溢出部分**永久英文**，直到后续 mutation 碰巧触及同一节点。
- **修复**：溢出时对溢出节点取最近未阻塞祖先（或直接 `translateSubtree(document.body)`）补一次全量扫描；注意与队列去重、防重复扫描护栏。

## C2【P0】删除确认句式默认注入"项目"名词，且结尾 `?` 被吞进名称 —✅ 实测复现

- **位置**：`localization_engine.js:664-670`
- **问题**：正则 `^Are you sure you want to delete (the |this )?(project group|project|workspace)?\s*(.+?)\??$` 中类型组为可选组，未命中时 `typeStr` 仍取默认 `"项目"`；且 `(.+?)\??$` 会把结尾 `?` 吞进名称捕获。
- **实测输出**：

| 输入 | 当前输出（错误） |
|---|---|
| Are you sure you want to delete **this rule**? | 您确定要删除 **项目 rule** 吗？ |
| Are you sure you want to delete **this project**? | 您确定要删除 项目 **?** 吗？ |
| Are you sure you want to delete **this workspace**? | 您确定要删除 工作区 **?** 吗？ |
| Are you sure you want to delete the project group My Group? | 您确定要删除 项目分组 My Group 吗？（正常） |

- **修复**：`typeStr` 默认置空；名称捕获改为 `(.+?)(?=\??$)` 或先剥离结尾问号再捕获；补 4 组回归用例（含 rule/automation/scheduled task 等非项目类型）。

## C3【P0】placeholder 属性翻译链在 `contextIsolation: true` 下确定失效 —✅ 官方包实证（本次最重要的升级结论）

- **位置**：`localization_engine.js:878`（TreeWalker 先 REJECT 后翻属性）、`:177`（`input`/`textarea` 在禁区选择器内）、`:763-772`（setAttribute 猴子补丁）、`:94`（互斥锁早退）
- **环境实证**：解包本机官方 `app.asar.bak` 读取配置——
  - `dist/utils.js:128`：主窗口 `webPreferences: { nodeIntegration: false, contextIsolation: true, ... }`
  - `dist/loadingOverlay.js:78`：加载遮罩 WebContentsView 同为 `contextIsolation: true`
  - `dist/preload.js`：通过 `contextBridge` 暴露 API（注释明确 "without nodeIntegration"）
- **推论**：preload 引擎运行在 **isolated world**。三条属性翻译路径的现状：
  1. TreeWalker：`BLOCKED_TAGS.has('INPUT')` 在调用 `translateElementAttrs` **之前** FILTER_REJECT → 初始扫描不翻 input 任何属性；
  2. MutationObserver attributes 分支：`closest(ALL_BLOCKED_SELECTOR)` 命中 `input` 选择器 → 动态属性变化也被跳过；
  3. 唯一兜底 `Element.prototype.setAttribute` 补丁：只作用于补丁所在 world，**主 world 框架的 `setAttribute` 调用走主 world 原型，拦截不到**；而主 world 的 executeJavaScript 注入引擎又被 `data-ag-i18n-active` 互斥锁在 `:94` 直接 return。
- **结论**：当前安装版本下，全应用 input/textarea 的 placeholder、输入框 aria-label 等属性翻译**确定不生效**（条 3 的补丁路径形同虚设，且互斥锁把主 world 兜底也堵死了），而非"未来可能失效"。
- **修复**：① 文本隔离与属性翻译解耦——REJECT 只拦文本，属性仍查表；② MutationObserver attributes 分支对 `placeholder` 豁免 input 排除（只排除用户输入值类属性）；③ 互斥锁区分"补丁安装标记"与"完整引擎启动标记"，主 world 注入时至少安装属性补丁；④ 通过 IDL 属性赋值（`el.placeholder = ...`）的路径无法用补丁拦截，只能由 observer 覆盖——所以 ①② 是必备。

## C4【P1】属性翻译覆盖集合三处不同步（data-* 断链）

- **位置**：`:762`（INTERCEPT_ATTRS 含 `data-tooltip/data-tip/data-title/data-balloon`）vs `:749`（translateElementAttrs 仅 3 属性）vs `:989`（attributeFilter 仅 3 属性）
- **问题**：页面初始已存在的 `data-*` 属性永不翻译；仅有补丁拦截的动态 `setAttribute` 会翻——而补丁又受 C3 的 world 限制。
- **修复**：三处集合统一；或按 C3 方案将属性翻译全部收敛到 observer attributes 路径。

## C5【P1】document.title / 窗口标题无覆盖

- **位置**：`:993`（observer 目标为 `document.body`，`<title>` 在 head 不扫描）；`TITLE` 不在 BLOCKED_TAGS 但无扫描路径；`document.title = "New chat — Antigravity"` 走 Document IDL setter，不触发任何补丁，且复合串（` — ` 拼接）无分段句式可命中。
- **佐证**：官方 `dist/utils.js` 存在 `page-title-updated` 截断处理器（>25 字符截断），说明标题动态变化频繁。
- **影响**：任务栏/Alt-Tab/系统标题栏保持英文或半英文。
- **修复**：新增分段句式 `^(.+?)\s*[-–—]\s*(.+)$`（分段查表后拼接）；监听 `document.title` setter / `<title>` 节点 characterData。

## C6【P1】动态句式锚定过严：标点/格式变体漏译 + catch-all 半翻译输出

- **位置**：`:455`（`^(\d+) tools? enabled$` 无 `\.?` 容忍，`"3 tools enabled."` 漏译，同类句式口径不一）；`:282`（`\(([0-9]+)\)` 不支持千分位 `(1,024)`）；`:542-595`（`Ran (.+)`/`Checked (.+)` 等 catch-all 在子串未命中时输出"已执行 node script.js"这类中英混串，且不进入 missedTexts，漏译采集工具永远看不到）。
- **修复**：补"标点变体矩阵"回归用例；catch-all 子串未命中时整体保持原样（与其余路径行为一致）。

## C7【P2】主进程 Hook 剩余盲区

- `new Menu()` + `popup()` 运行时菜单（右键菜单若不经 buildFromTemplate 构建）不在包装范围——只包了 `setApplicationMenu/buildFromTemplate`（`:1185-1200`）与 Tray（`:1205-1218`）；
- `translateDialogOptions`（`:1151-1168`）未覆盖 `checkboxLabel`、`nameFieldLabel`；
- 渲染进程 `window.alert/confirm` 与 web Notification API 不经过已包装的 `electron.Notification`/`dialog`。
- **修复**：`safePatch(Menu.prototype, 'popup')`；扩展 dialog 选项字段；渲染层 hook `window.alert/confirm`。

## C8【P2】主进程与渲染层动态句式两套实现且严重不对等

- 主进程 `translateText`（`:1090-1112`）仅 4 条动态规则，渲染层 40+ 条 → 原生菜单/托盘/对话框的动态文案覆盖面明显更弱，且两处规则漂移是必然维护风险。
- **修复**：抽取同一份动态句式表，主/渲染共同生成。

## C9【P2】空格保真只保单空格

- **位置**：`:826-827`：`leadingWs/trailingWs` 只保留一个空格，多空格缩进在 pre-line/pre-wrap 非禁区容器内被塌缩，列对齐微移。

## C10【P2】框架拆分文本节点是结构性覆盖上限

- 逐节点查表无法处理插值拆分（"You have" + N + "files left"）；`:416-421` 的裸 `to` 特判是唯一父级上下文兜底，且存在误判风险（祖先文本含 `master|main|branch|changes|commit` 即翻译孤立 `to` 节点）。
- 记录为已知上限，不建议扩大特判面。

## C11【P2】主进程注入 URL 白名单为子串匹配

- **位置**：`:1319`：`url.includes('localhost')` 是全文匹配，`https://evil.com/?x=localhost` 也会放行注入。
- **修复**：解析 hostname 判断（`new URL(url).hostname === '127.0.0.1' || 'localhost'`）。

---

# 二、安全区隔离覆盖面与精确度

## S1【P0，风险型】AI 流式正文隔离完全押在类名枚举上，无结构级兜底

- **位置**：`AI_STREAM_PROSE_SELECTOR`（`:136-155`）：`.prose/.markdown-body/.animate-markdown/[data-thought]` 等十余个逆向类名。
- **风险 1（隔离盲区）**：客户端一次改版换掉正文容器类名（如 `[class*="chat-markdown"]`、`.md-content`、`[class*="message-content"]`），整片 AI 输出脱离隔离 → AI 英文句子会直接撞上 `^Updated (.+)`、`^Run (.+)`、`^Ran (.+)`、`^(.+?) finished` 等**宽泛动态正则被改写**（如 AI 输出 "Updated the config" → "更新于 the config"），产物损坏。
- **风险 2（过隔离）**：`.prose` 等全局选择器同时误伤设置页/帮助页/更新日志中复用 `.prose` 的静态可翻译内容 → 覆盖损失。
- **佐证**：`scratch/dump_missing.js:102` 的内部 blockedRe 指纹（`message-content|chat-message|snippet|trajectory|step-|token` 等）比引擎清单**更宽**——工具与引擎口径已不一致，可作为扩展蓝本。
- **修复**：① 扩指纹（`[class*="markdown"]`、`[class*="rich-text"]` 等，宁宽勿窄）；② **加第二道防线**：长句（含 ≥N 个空格或句末标点）只允许精确字典命中、跳过全部动态正则；③ 结构级识别（AI 消息容器 role/aria + `[data-streaming]` 祖先）；④ 用 dump_missing 的 blocked/untranslated 双向清单定期实证。

## S2【P1】用户内容未整体隔离

- **位置**：`:188-191` 只挡 `[data-turn-role="user"]` 下的 `.whitespace-pre-wrap`/`pre`/`code`；`:193-194` 只挡 `[class*="truncate"]` 的会话标题。
- **问题**：用户英文提问的普通段落（非 pre-wrap）、非 truncate 结构的会话/项目名会被翻译——**用户自命名内容与用户输入被"篡改"**，属于安全区隔离覆盖面缺口。
- **修复**：`[data-turn-role="user"]`、`[data-message-author="user"]` 整树入禁区；会话/项目标题增加结构级选择器。

## S3【P1】属性猴子补丁无禁区检查（隔离在属性路径上被绕过）

- **位置**：`:763-772`：补丁只查是否含中文，**不查** `closest(ALL_BLOCKED_SELECTOR)`。
- **问题**：终端/Monaco/prose 禁区内的元素在赋值 `title/aria-label` 时照样被翻译，文本路径的整树隔离在属性路径上不成立（与 C3 的"REJECT 拦文本不拦属性"修复方案一体两面，需统一设计：**禁区内文本不翻，禁区内属性是否翻属策略选择，须明确口径**）。

## S4【P2】整树过挡 → 覆盖面损失（三个层级）

- `[class*="tool-call-result"]`、`[class*="terminal-output"]`（`:198`）：结果卡片内的 UI 铬层（Exit code / Duration / "Output" 标签等可翻译元数据）连带被挡；
- `pre`/`code` 全量标签级熔断（`:170`）：行内 `code` 标签（版本号、参数名、键帽文字）与 `pre` 静态说明块全部保持英文；
- `.prose` 全局选择器对静态文章页的误伤（见 S1 风险 2）。
- **建议**：工具调用卡片只挡内部输出内容区；统计后决定 `code` 是否放宽到"仅块级"。

## S5【P2】子串选择器过杀

- `[class*="lines-content"]`、`[class*="terminal-rows"]` 等 `[class*=]` 匹配可能命中 `task-lines-content` 这类业务容器 → 整树熔断。建议用 dump 工具的 blocked 清单逐条实证每个子串选择器的实际误伤面后收敛。

## S6【P2】Shadow DOM：穿透判定枚举式；深层宿主不翻译是已锁定的设计边界

- **位置**：`:864-870` 仅在传入 root 恰为 shadow host 时进入一次；TreeWalker 不穿 shadow 边界；MutationObserver 挂 body、**不跨 shadow 边界**（shadow 内后续动态更新永不重译，深层 shadow host 初始扫描也不覆盖）。
- **边界确认**：`engine_fix_test.js:200-225` 将"深层宿主 shadow 内容不翻译"锁定为测试用例（期望行为）——**改动前须先确认设计意图**，勿当纯 bug 修复。
- 穿透条件 `hostCls.includes('xterm'/'terminal'/'monaco')` 为枚举式字符串判断，类名含 `cm-`/`code-editor`/`diff` 的代码宿主组件存在被穿透风险（内部结构通常能二次命中 ALL_BLOCKED，双保险，风险低）。建议升级为 `host.matches(ALL_BLOCKED_SELECTOR)` 判定。

## S7【P2】`isMutating` 自激锁在 MutationObserver 时序下基本失效

- **位置**：`:934`。MutationObserver 回调是微任务，在 `finally` 复位（`:839`）之后才执行，故回调顶部 `if (isMutating) return` 几乎总是 false——自激防护实际由 WeakMap 缓存兜底。无实害，但注释与实现不符，属防御逻辑失效。

## S8【P2】物理特征防御小项

- `:815` `^[0-9a-f]{7,40}$` 对英文 hex 词（"facade"、"deadbeef" 类 7 位以上全 hex 字母词）有小概率误杀；
- `:816` `^--?[a-zA-Z0-9_-]+$` 对单连字符普通词同样小概率误杀（当前风险低，因为还需字典命中才会改写）；
- `:813` 扩展名清单缺 `.txt/.log/.env`（实际风险低）；
- README 写 BLOCKED_TAGS 为 22 个，实测 **23 个**（SCRIPT,STYLE,CODE,PRE,INPUT,TEXTAREA,SVG,CANVAS,SYMBOL,PATH,KBD,SAMP,VAR,TEMPLATE,MATH,AUDIO,VIDEO,SOURCE,TRACK,IFRAME,OBJECT,EMBED,NOSCRIPT）——文档需修正。

---

# 三、复核记录（两轮评审差异处理）

| 事项 | 处理 |
|---|---|
| GLM：队列溢出无兜底（P0） | ✅ 已复核确认，采纳为 C1 |
| GLM：删除确认默认名词 + 结尾 ? 吞噬（P0） | ✅ 已实测复现且更严重，采纳为 C2（补了 `?` 被吞捕获的次级 bug） |
| GLM：属性补丁无禁区检查 / URL 子串 / dialog 字段盲区 / Menu.popup | ✅ 已复核确认，采纳为 S3 / C11 / C7 |
| GLM：**contextIsolation:false 断言** | ❌ **错误，方向相反**：官方包实测为 `contextIsolation: true`。该事实升级为 C3 的确定性结论，而非"未来风险" |
| GLM：shadow 只进一层（当缺陷提） | ⚠️ 部分成立，但深层宿主不翻译是 `engine_fix_test.js` 锁定的设计边界，改动前须确认意图（S6） |
| 本轮独有：用户内容未隔离（S2）、标点变体锚定过严（C6）、互斥锁堵死兜底（C3 修复②③）、WEBVIEW 不在黑名单、主/渲染规则漂移（C8） | 保留并入正文 |
| 双方一致且验证成立的其余条目 | 并入正文相应编号 |

---

# 四、合并修复优先级

| 优先级 | 条目 | 一句话要点 |
|---|---|---|
| **P0** | C1 | 队列溢出补 `translateSubtree` 兜底（一行，README 承诺兑现） |
| **P0** | C2 | 删除确认：默认名词置空 + 修复 `?` 吞噬 + 4 组回归用例 |
| **P0** | C3 | 属性翻译与文本隔离解耦；observer 对 placeholder 豁免 input；互斥锁区分补丁/引擎标记（官方包实证 contextIsolation:true，当前确定失效） |
| **P0（风险型）** | S1 | 扩 AI 正文指纹 + 长句只走精确查表 + 结构级识别；dump 实证后定级 |
| **P1** | C4 | 属性覆盖集合三处统一 |
| **P1** | C5 | document.title 分段句式 + setter 监听 |
| **P1** | C6 | 标点变体矩阵；catch-all 未命中保持原样 |
| **P1** | S2 | 用户消息容器整体隔离 |
| **P1** | S3 | 属性补丁加禁区检查（与 C3 统一口径） |
| **P2** | C7/C8/C9/C10/C11、S4/S5/S6/S7/S8 | 按正文逐项 |

---

# 五、代码位置索引

| 条目 | 位置 |
|---|---|
| BLOCKED_TAGS（23 个） | `localization_engine.js:129-133` |
| AI_STREAM_PROSE_SELECTOR | `:136-155` |
| FORBIDDEN_SUBTREE_SELECTOR / ALL_BLOCKED_SELECTOR | `:158-199` / `:202` |
| translateElementAttrs（3 属性） | `:747-756` |
| INTERCEPT_ATTRS（7 属性） | `:762` |
| setAttribute / title 补丁（无禁区检查） | `:759-792` |
| 删除确认句式（C2） | `:664-670` |
| catch-all 句式（C6） | `:542-595` |
| TreeWalker INPUT 先 REJECT（C3） | `:878` |
| shadow 单层穿透（S6） | `:864-870` |
| pendingQueue 上限（C1） | `:946` / `:954` |
| observer attributes 分支（C3） | `:958-966` |
| attributeFilter（C4） | `:988-990` |
| 互斥锁早退（C3） | `:94-99` |
| 主进程 translateText 4 规则（C8） | `:1090-1112` |
| 主进程注入 URL 过滤（C11） | `:1311-1326`（`includes('localhost')` 在 `:1319`） |
| 官方包 webPreferences（C3 实证） | 本机 `app.asar.bak` → `dist/utils.js:128`、`dist/loadingOverlay.js:78` |

*（本报告完成后临时解包目录已清理，未改动任何项目代码与客户端文件。）*